#include <stdio.h>
#define pie 3.14
int main() {
   // printf() displays the string inside quotation
   // variables and data types in C
   //int pie = 10.5 // we connot do this as pie is constant
   int num = 25;
   //const float pie = 3.14;
   // pie = 10.7 // We cannot do this as pie is constant
   //printf("Hello, World! %d", num);
   printf("Value of float constant is %f\n", pie);
   return 0;
}
